If (mv_par03)=1   
	cQuery += " Group by "+cCampos //Muitos	  
	cQuery += " Having count(*) > 1" 
	ElseIf (mv_par03)=2
	cQuery += " and ct1_conta = '"+" "+"'" //null
Endif

If (mv_par04)=2   
	cQuery += " Group by "+cCampos //Muitos	  
	cQuery += " Having count(*) > 1" 
	ElseIf (mv_par04)=4
	cQuery += " and ct1_conta = '"+" "+"'" //null
Endif
